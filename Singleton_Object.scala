object Singleton_Object {
// main method must be in singleton object
  // object can be extend to the class but class can't extend to the object
  // object must be run the program on static member and method

  def main(args: Array[String]): Unit = {
    print("Hello world")
  }
}
